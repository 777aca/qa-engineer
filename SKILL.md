---
name: qa-engineer
description: 面向测试工程师的用例设计、API/UI 自动化、性能与 App 测试、缺陷定位和报告工作流。用户要求测试功能、设计测试方案、排查失败或基于 URL/源码探索 Bug 时使用；仅提供链接或代码且意图不明时先确认目的。支持结构化用例与 Excel、XMind 等报告。
metadata:
  author: 波波啾
---

# QA Engineer Skill

围绕用户指定的业务目标建立可执行检查，按证据报告实际结果。按测试层级组织资料，沿用被测项目的技术栈、脚本与目录约定。

## 接收任务

先从对话和已有材料提取：任务目标、被测范围、可用材料、输出格式、平台、测试环境与允许的操作。已经明确的信息直接复用，只问会影响结果的缺项；不要求每次填写完整问卷。

- 纯知识咨询直接回答。
- 用户明确要求测试 URL 或源码时进入探索流程；仅粘贴 URL/源码并不自动授权测试。
- 探索默认 L2、PC；明确提及移动端或使用移动端框架时采用对应平台并说明。用户指定的档位、平台和功能范围优先。
- 环境或操作权限不明时可以先读代码、检查页面结构；执行登录、提交、上传、修改、删除、支付或安全探针之前复用已有授权，缺少必要授权才追问。档位本身不是授权。
- 没有材料时列出最少需要补充的内容，不编造业务规则。
- 可以使用宿主实际提供的提问工具；没有专用工具时直接用简短文字询问，不依赖固定工具名称。

## 默认交付

实际测试、用例设计、测试计划或缺陷排查任务，默认包含可复用的测试用例清单；用户明确说“只要某产物”或“不要用例”时遵从。

- 用户指定输出格式时按其要求；探索默认用例清单和 Bug 汇总两份 Excel。纯咨询不创建文件。
- 用例定义与执行记录分开理解：设计出的用例可未执行；没有执行断言不能标 Pass。
- 统一字段为 `id / title / module / priority / preconditions / steps / expected`。执行后增加 `result / actual / level / platform / linked_bug / evidence`，见 [输出契约](references/output-formats.md)。
- 正式交付遵循项目 `docs/` 约定，临时评审遵循 `discuss/`；用户指定目录优先。独立扫描默认写入忽略版本控制的 `samples/out/`。
- PC 和 Mobile 保留独立执行记录。相同根因的缺陷可汇总，但不能把某一平台的失败合并成另一平台的通过。

## 工作流

1. **确定预期**：优先采用需求、接口契约、已有验收测试和用户说明。页面或源码推断属于假设，缺少依据时记录待核实项。
2. **选择范围**：按下表只加载相关参考。档位决定检查深度，不强制扩大用户选择的功能范围。
3. **建立检查点**：明确前置条件、操作、可观察的预期结果与所需权限。没有明确结果断言的动作不能作为通过的用例。
4. **执行并取证**：复用项目现有工具；探测依赖是否可用。按场景隔离浏览器状态、账号和测试数据；网络/定位失败要留状态，不能吞掉。
5. **复核发现**：记录预期与实际、最小复现步骤、平台、证据和影响。规则猜测与运行时复现分开报告；一次观察不能标“必现”。
6. **交付覆盖记录**：同时列出通过、失败、跳过、阻塞、执行错误与待复核项，说明未覆盖原因。默认转换为用例清单和缺陷汇总；不自动提交 Git、提工单或发送消息。

## 按需加载

| 任务 | 参考 |
|---|---|
| 需求分析、用例设计、测试计划 | [test-design.md](references/test-design.md) |
| HTTP/REST/GraphQL 接口测试 | [api-testing.md](references/api-testing.md) |
| 浏览器 UI/E2E 自动化 | [ui-e2e-playwright.md](references/ui-e2e-playwright.md) |
| 性能与压力测试 | [performance-testing.md](references/performance-testing.md) |
| 移动 App、Flutter、UniApp | [app-testing.md](references/app-testing.md) |
| 单个 Bug 复现与正式报告 | [bug-report.md](references/bug-report.md) |
| URL/源码探索 | [exploratory-testing.md](references/exploratory-testing.md) |
| Excel/XMind/Word/PPT 等交付 | [output-formats.md](references/output-formats.md) |

## 判断与执行约束

- 聚焦功能正确性、数据和业务状态；不主动扩展成命名、代码风格或目录规范审查。
- Happy Path、异常、边界、并发与兼容性是按任务选择的维度；不要求 L0/L1 或单按钮测试覆盖全部维度。
- 账号框可以是 email/tel/text；空值禁用按钮、原生表单校验、装饰图空 alt 都可能是正确设计。
- 不把短 HTML 判为白屏，不凭 URL 改变判定登录成功，不把多次请求直接等同于业务重复，不猜测密码长度与去空格规则。
- 优先使用角色、标签或稳定测试标识定位，操作前确认实际存在；禁止用猜测坐标代替未识别的控件。
- 用元素状态、响应事件或业务断言等待；不依赖固定休眠和 networkidle 判断测试成功。
- 凭据从受控环境变量或本地登录态读取；报告不保存明文或凭据前缀。截图遮罩输入框及指定敏感区域；原始 Trace 属于私密调试文件，不能自动分享。
- 对破坏性操作、付费提交、安全测试和压力测试采用已有授权范围。没有授权的检查记录 Blocked，不用重试绕过限制。
- 不能运行环境时交付静态发现与未执行用例；区分“静态证明”“运行时复现”“待验证假设”，不宣称已跑过。

## 可复用脚本

所有脚本路径相对本 Skill 根目录；实际运行时解析成完整路径，产物写入用户的项目目录。不要为了使用脚本强制复制一份到被测项目。

- `scripts/scan.py`：页面基础检查与声明式业务场景；保留 L0–L4、PC/Mobile。使用前读取 [扫描器使用说明](docs/scan-runner.md)。
- `scripts/api_smoke.py`：按 YAML/JSON 执行接口冒烟；配置只使用获授权的测试地址和操作。
- `scripts/new_test_case.py` / `scripts/new_bug_report.py`：生成单条模板。
- `scripts/cases_to_xlsx.py` / `scripts/cases_to_xmind.py` / `scripts/bugs_to_xlsx.py`：先校验数据再导出，支持旧 precondition 字段的无冲突迁移。
- `scripts/test.py`：本仓库回归测试，只访问本机测试页面。

安装及开发验证见 README。外部文档生成能力按宿主实际安装的工具选择；缺失时说明依赖与可用替代格式，不虚构已生成的文件。
